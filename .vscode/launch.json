{
  // VS Code Debug-Konfigurationen
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug pdfreorder",        // Anzeigename im Debug-Menü
      "type": "go",                      // Go-Debugger (Delve)
      "request": "launch",               // Startet das Programm
      "program": "${workspaceFolder}",   // Hauptpaket liegt im Projektordner
      "args": [
        "-in", "SWM-------------------------------_C51.pdf",            // Eingabedatei (Pfad anpassen!)
        "-out", "ausgabe_duplex.pdf"     // Ausgabedatei
      ],
      "cwd": "${workspaceFolder}",       // Arbeitsverzeichnis
      "env": {}
      //"console": "integratedTerminal"    // Ausgabe im VS Code-Terminal
    }
  ]
}